'use strict';

/**
 * Player Configuration.
 */
export const PLAYER = {
    walkSpeed: 1,
    runSpeed: 2,
    lookSpeed: 70 // mouse-delta / lookSpeed
};

/**
 * Map config.
 */
export const MAP = {
};

/**
 * Debug settings.
 */
export const DEBUG = {
    player: false,
    flatMap: false
};